{
  "name": "cypress1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cypress: test": "cypress run --spec cypress/integration/firstLection/booksManage.cy.js",
    "test": "npx cypress run -b chrome --headed",
    "headTestChrome": "npx cypress run -b chrome --headed -C cypress.env.json",
    "headlessTestChrome": "npx cypress run -b chrome",
    "headTestElectron": "npx cypress run -b electron --headed -C cypress.json",
    "headlessTestElectron": "npx cypress run -b electron"
  },
  "author": "Vavlauz",
  "license": "ISC",
  "dependencies": {
    "cypress": "^10.4.0"
  }
}
